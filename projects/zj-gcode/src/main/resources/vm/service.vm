package ${data.packageName}.impl.service;

import ${data.packageName}.to.${data.toName};

public interface ${data.toName}Service {
    int save(${data.toName} ${data.lowerToName});

    ${data.toName} selectByPrimaryKey(${data.primaryField.fieldTypeString} ${data.primaryField.fieldName});

    int delete(${data.primaryField.fieldTypeString} ${data.primaryField.fieldName});

    int update(${data.toName} ${data.lowerToName});
}