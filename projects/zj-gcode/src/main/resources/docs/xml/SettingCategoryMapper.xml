<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ouyang.test.impl.dao.SettingCategoryMapper">
    <resultMap id="BaseResultMap" type="SettingCategory">
        <id column="ID" property="id" />
        <result column="NAME"  property="name" />
        <result column="SORT"  property="sort" />
    </resultMap>

    <sql id="Base_Column_List">
        ID,NAME,SORT
    </sql>

    <insert id="save" parameterType="SettingCategory">
        insert into SETTING_CATEGORY
        <trim prefix="(" suffix=")" suffixOverrides=",">
             <if test="id != null">
                ID,
             </if>
             <if test="name != null">
                NAME,
             </if>
             <if test="sort != null">
                SORT,
             </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
             <if test="id != null">
                 #{id},
             </if>
             <if test="name != null">
                 #{name},
             </if>
             <if test="sort != null">
                 #{sort},
             </if>
        </trim>
    </insert>

    <delete id="delete">
        DELETE FROM SETTING_CATEGORY WHERE ID = #{id}
    </delete>

    <update id="update" parameterType="IosChannel">
        update SETTING_CATEGORY
        <set>
            <if test="id != null">
                ID = #{id},
            </if>
            <if test="name != null">
                NAME = #{name},
            </if>
            <if test="sort != null">
                SORT = #{sort},
            </if>
        where ID = #{id}
    </update>

    <select id="selectByPrimaryKey" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/>
        from SETTING_CATEGORY
        where ID = #{id}
    </select>
</mapper>